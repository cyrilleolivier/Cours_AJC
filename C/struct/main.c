#include <stdio.h>

typedef struct _livre{
    char auteur[30];
    char titre[30];
    short an ;
    struct _livre *suivant;
} Livre;

int main()
{
    Livre *p1 =(struct_livre *)malloc(sizeof (struct_livre);
            strcopy(Livre->auteur,"jean");
    suivant ->null;

    return 0;
}
